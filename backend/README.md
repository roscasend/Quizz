# Backend

Spring Boot microservices live here.

Current services:

```text
quiz-service
```

Planned services:

```text
api-gateway
identity-service
attempt-service
notification-service
analytics-service
```
