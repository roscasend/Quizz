# Infrastructure

Shared platform infrastructure will live here later, for example Kubernetes manifests, observability dashboards, and environment documentation.

Backend-local Docker Compose files live under `backend/` so backend developers can work without this folder.
